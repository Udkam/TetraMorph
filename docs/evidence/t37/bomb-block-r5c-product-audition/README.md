# R5C — current product block-break audition

This source-only page binds the current production `AudioEngine` and `TetrisRenderer` to two
real Core fixtures: normal Bomb and a row-39 chain-clear. It contains no R5B A/B/C selector,
archived Bomb WAV fetch, autoplay, persistence, or acceptance control.

Full-motion review is `正常 Bomb → 连锁清屏`; direct scene controls are only for repeatable
technical capture. The reduced-motion control is technical-only. Browser state exposes the
single 48 kHz mono Bomb buffer/source, exact compositor equality, active-catalog asset audits,
and live owner cleanup so that later final-candidate evidence can be reproduced without
reinterpreting subjective acceptance.

Generated screenshots and browser reports are intentionally not created by this source slice.
They are produced only from the final integration candidate. Exact certificates for the deferred
Horizon, Terrace, and Keystone proof work are not an input to this audition and must not be claimed
by its generated evidence.
